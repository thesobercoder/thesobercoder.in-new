---
import type { Props } from "./props";

const { href, rel, target, ariaLabel } = Astro.props;
---

<a
  data-ccursor
  href={href}
  rel={rel}
  target={target}
  aria-label={ariaLabel}
  class="w-28 group relative inline-flex items-center justify-center overflow-hidden rounded-xl bg-neutral-700 py-3 transition"
>
  <div class="absolute inset-0 flex items-center justify-center">
    <div
      class="absolute h-[250%] w-[250%] left-[-75%] top-[-75%] animate-spin bg-[conic-gradient(from_0_at_50%_50%,rgba(255,255,255,0.5)_0deg,transparent_60deg,transparent_300deg,rgba(255,255,255,0.5)_360deg)] opacity-0 transition duration-300 [animation-duration:3s] group-hover:opacity-100"
    >
    </div>
  </div>
  <div
    class="absolute inset-0.5 rounded-[calc(theme(borderRadius.xl)-2px)] bg-neutral-900"
  >
  </div>
  <div
    class="absolute rounded-lg bottom-0 left-1/2 h-1/3 w-4/5 -translate-x-1/2 bg-white/10 opacity-50 blur-md transition-all duration-500 group-hover:h-2/3 group-hover:opacity-100"
  >
  </div>
  <span
    class="relative mt-px bg-gradient-to-b from-white/25 to-white bg-clip-text text-md font-medium text-transparent transition-all duration-200"
  >
    <slot />
  </span>
</a>
